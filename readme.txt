
### INSTALL REQUIREMENTS ###
To setup the tool, create a new virtual and install the dependencies:
pip install -r requirements.txt

### RUN CODE ###
to run the code, launch the following comand:
python main.py /path/to/file/file_name.root path/to/output/
